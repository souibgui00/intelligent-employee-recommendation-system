# Organisation des scripts backend

- `utils/` : scripts utilitaires généraux
- `migrations/` : scripts de migration de base de données
- `tests/` : scripts de test
- `diagnostics/` : scripts de diagnostic
- `seeders/` : scripts d’initialisation de données
- `pdf/` : scripts liés au traitement PDF
