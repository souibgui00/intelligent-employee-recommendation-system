import { Test, TestingModule } from '@nestjs/testing';
import { SkillDecayController } from './skill-decay.controller';

describe('SkillDecayController', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
